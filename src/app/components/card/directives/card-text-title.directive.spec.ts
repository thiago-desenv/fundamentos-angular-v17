import { CardTextTitleDirective } from './card-text-title.directive';

describe('CardTextTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new CardTextTitleDirective();
    expect(directive).toBeTruthy();
  });
});
